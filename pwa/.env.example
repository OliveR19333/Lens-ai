# PWA build-time config. Copy to .env. Vite exposes VITE_* vars to the client.
VITE_API_BASE_URL=http://localhost:8080
# MapLibre style URL (offline-capable vector basemap, spec §3.1).
VITE_MAP_STYLE_URL=https://demotiles.maplibre.org/style.json
