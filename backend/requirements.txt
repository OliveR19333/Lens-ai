# GAS Property Mapping System — backend dependencies
# Python 3.11+

# --- Web framework ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # multipart image upload
pydantic>=2.7
pydantic-settings>=2.2

# --- Auth ---
pyjwt>=2.8
passlib[bcrypt]>=1.7

# --- Database / spatial ---
sqlalchemy>=2.0
geoalchemy2>=0.15             # PostGIS column types
psycopg2-binary>=2.9
alembic>=1.13                 # migrations

# --- Task queue ---
celery[redis]>=5.4
redis>=5.0

# --- Geo / raster processing ---
geopandas>=0.14
pyogrio>=0.8                  # fast shapefile -> GeoJSON
shapely>=2.0
rasterio>=1.3
numpy>=1.26

# --- AI feature detection (Phase 3) ---
ultralytics>=8.2             # YOLOv8
torch>=2.2                   # CPU build is fine for Mac

# --- Map rendering / output ---
Pillow>=10.3
matplotlib>=3.8
reportlab>=4.1               # PDF generation

# --- HTTP ---
httpx>=0.27
requests>=2.31

# --- Dev / test ---
pytest>=8.2
